use overbejt;
create table geneII(
    ENSMBLE_VERSION INT NOT NULL,
    SEQ_NAME VARCHAR(250),
    SOURCE_ID VARCHAR(250),
    FEATURE VARCHAR(250),
    START_INDEX INT,
    END_INDEX INT,
    SCORE INT,
    STRAND VARCHAR(250),
    FRAME INT,
    TRANSCRIPT_VERSION VARCHAR(250),
    TRANSCRIPT_SOURCE VARCHAR(250),
    TRANSCRIPT_BIOTYPE VARCHAR(250),
    TRANSCRIPT_SUPPORT_LEVEL VARCHAR(250),
    GENE_VERSION VARCHAR(250),
    EXON_ID VARCHAR(250),
    CCDS_ID VARCHAR(250),
    TRANSCRIPT_NAME VARCHAR(250),
    EXON_VERSION VARCHAR(250),
    TRANSCRIPT_ID VARCHAR(250),
    GENE_NAME VARCHAR(250),
    PROTEIN_ID VARCHAR(250),
    PROTEIN_VERSION VARCHAR(250),
    TAG VARCHAR(250),
    GENE_SOURCE VARCHAR(250),
    GENE_BIOTYPE VARCHAR(250),
    GENE_ID VARCHAR(250),
    EXON_NUMBER VARCHAR(250),
    INDEX (GENE_KEY, ENSMBLE_VERSION, SEQ_NAME, SOURCE_ID, FEATURE, START_INDEX, END_INDEX, SCORE, STRAND, FRAME, TRANSCRIPT_VERSION, TRANSCRIPT_SOURCE, TRANSCRIPT_BIOTYPE, TRANSCRIPT_SUPPORT_LEVEL, GENE_VERSION, EXON_ID, CCDS_ID, TRANSCRIPT_NAME, EXON_VERSION, TRANSCRIPT_ID, GENE_NAME, PROTEIN_ID, PROTEIN_VERSION, TAG, GENE_SOURCE, GENE_BIOTYPE, GENE_ID, EXON_NUMBER)
);
